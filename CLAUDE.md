# Security Plugin 1 — プロジェクトコンテキスト

このファイルはClaude Code用の恒久的な指示書です。毎セッションの開始時に読み込まれます。

## プロジェクト概要

WordPress向けの軽量セキュリティプラグイン。コンセプトは「使っているプラグインの危険を、専門用語なしの日本語で教えてくれる、軽いセキュリティ番」。
フリーミアムのサブスク事業として開発する。無料版は設定診断、Pro版はCVE脆弱性アラート・更新代行・定期通知を担う。
ターゲットは日本の個人ブロガー・小規模事業者・Web制作会社（非エンジニア中心）。

## リポジトリ規約

- リポジトリ: Teeeda112923/Security_plugin1（main ブランチ）
- 関数・定数の接頭辞: `SP1_`（定数）/ `sp1-`（スラッグ・ハンドル）
- Text Domain: `security-plugin1`
- バージョン: v0.1.0 開発中
- `your-repo` への書き込みは禁止

## 設計方針（必ず守る）

- **自動で変更しない**: サイトの設定やファイルを勝手に書き換えない。診断と通知にとどめ、判断は利用者に委ねる。必要なら標準の設定画面へ案内するボタンを置く程度。
- **平易な日本語**: 専門用語を避ける。使う場合はかっこ書きで噛み砕く。
- **軽量設計**: 重いリアルタイムスキャン、自前WAF、マルウェアスキャンは作らない。サイト速度を落とさない。
- **無料版は外部通信なし**: 無料の診断はサイト内の状態とWordPress組み込み情報の読み取りだけで完結させる。
- **推測で実装しない**: DB設計・通知の仕組み・Pro版の内部実装は、仕様が確定するまでコードを書かない。不明点は実装せず確認する。
- 「簡素化」という語は使わない（別の表現に置き換える）。

## 無料版とProの線引き（設計の背骨）

判定に必要なデータの所在で機能の所属を決める。

- サイト内の状態だけで判定できる → **無料**
- 外部の脆弱性データベースとの突合が必要 → **Pro**
- 定期実行・メール通知が必要 → **Pro**

例: 「更新が来ているか」は無料。「その更新が脆弱性修正か・どれだけ危険か」はPro。

## 無料版の診断項目（MVP・全10項目）

すべて good / attention / recommended の三段階で表示する。

カテゴリA（バージョン鮮度・3項目）
- WordPress本体が最新か（メンテナンス版未適用なら recommended）
- PHPバージョン（8.4以上=good / 8.2・8.3=attention / 8.1以下=recommended。日付直書きを避けメジャー番号で判定し、更新時に見直す）
- プラグイン・テーマの未更新件数（標準の更新画面へ案内。プラグイン自身は更新を実行しない）

カテゴリB（ハードニング設定・7項目）
- 本番でのデバッグ表示（WP_DEBUG）※既存実装
- 管理画面からのファイル編集（DISALLOW_FILE_EDIT）
- 管理者ユーザー名が既定名か
- 常時HTTPS（SSL）
- データベース接頭辞が既定値か
- XML-RPC の状態
- REST API のユーザー名列挙

## 表示ルール

- 各項目に「なぜ問題か」を一文、「どうすればよいか」を一手順だけ添える。
- recommended（強い警告）は「漏れる・壊される・盗まれる」に該当するものだけ。具体的にはデバッグ表示・ファイル編集・常時HTTPS。
- 既存サイトで変更にリスクがある項目（管理者ユーザー名・DB接頭辞）は attention 止まりにし、変更を急かさない。
- 文面は翻訳ファイル（Text Domain: security-plugin1）に登録する。

## 設計ドキュメント

詳細は docs フォルダを参照（必要なときに読むこと。常時読み込みは不要）。
- docs/business-design.md … 事業設計（市場・価格・ロードマップ）
- docs/free-tier-scope.md … 無料版スコープと無料/Proの線引き
- docs/categoryA-spec.md … バージョン鮮度の判定しきい値と文面
- docs/categoryB-spec.md … ハードニング設定の判定しきい値と文面

## 現在の実装状況

- v0.1.0。デバッグ設定診断（good/attention/recommended）のみ実装済み。
- 次の目標: カテゴリBを既存のデバッグ診断の延長で拡張 → カテゴリA → 全文面の翻訳ファイル登録 → 公式リポジトリ申請用パッケージ化。
